name: Wrapper generation

on:
  push:
    branches: [main]
    paths: ['spec.yaml']
  workflow_dispatch:

jobs:
  trigger-wrappers:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger PHP Wrapper
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.DISPATCH_TOKEN }}
          repository: BuiltByBit/api-wrapper-codegen-php
          event-type: spec-updated

      - name: Trigger Java Wrapper
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.DISPATCH_TOKEN }}
          repository: BuiltByBit/api-wrapper-codegen-java
          event-type: spec-updated

      - name: Trigger NodeJS Wrapper
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.DISPATCH_TOKEN }}
          repository: BuiltByBit/api-wrapper-codegen-nodejs
          event-type: spec-updated
